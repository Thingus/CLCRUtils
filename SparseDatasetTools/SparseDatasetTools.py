# -*- coding: utf-8 -*-
"""
Created on Tue Sep 26 16:07:10 2017

A set of tools for working with sparse geographic data sets

@author: John Roberts
"""

import tempfile as tf

def CompressSparseCrutem(fileIn, fileOut, missingValue):
    with open(fileIn, 'r') as fileIn, open(fileOut, 'r') as fileOut:
        dsfsadf
        
def test_CompressSparseCrutem():
    
    
    
    
    
    
"""
    row = 0 #counts the data blocks of 72 values per row
    print('Reading data file: '+crutemfile)
    for line in f:
        line = line.strip() #remove \n
        columns = line.split() #split string into words
        if float(columns[0]) > 1800: #test whether we have reached a header line
            year = int(columns[0])
            month = int(columns[1])
            row = 0 #reset row counter
        else:
            t = [float(x) for x in columns] #convert list of strings to list of floats, with temperatures for 72 columns of one row
            ti = (year-1850)*12 + month-1 #time index for array
            crutem4v[ti,row,:] = t
            row = row+1 #next row
    f.close()
"""
    
    
        